def cross_product(x, y):
    product0 = 0
    product1 = 0
    product2 = 0
    if(len(x)==len(y)==3):
        product0 = (x[1]*y[2])-(x[2]*y[1])
        product1 = -((x[0]*y[2])-(x[2]*y[0]))
        product2 = (x[0]*y[1])-(x[1]*y[0])
        result_p = (product0, product1,product2) 
        return result_p
    else:
        return("use maths,not normal cross product")
def reciprocal_vector(a1, a2, a3):
    b1=0
    b2 = 0
    b3=0
    volume = 0
    
    
    
    if(len(a1)==len(a2)==len(a3)==3):
        volume = a1[0]*((a2[1]*a3[2])-(a2[2]*a3[1])) - a1[1]*((a2[0]*a3[2])-(a2[2]*a3[0])) + a1[2]*((a2[0]*a3[1])-(a2[1]*a3[0]))
        b1 = (cross_product(a2, a3))
        b2 = (cross_product(a3, a1))
        b3 = (cross_product(a1, a2))
        print("the volume, b1 , b2 and b3 vectors are as follows")
        return[volume, b1, b2, b3]
    else:
        return("no possible")
a1 = [1, 1, -1]
a2 = [1, -1, 1]
a3 = [-1, 1, 1]
print(reciprocal_vector(a1, a2, a3))
